fastapi>=0.115
uvicorn[standard]>=0.30
anthropic>=0.100
httpx>=0.27
pydantic>=2.7
google-auth>=2.30
requests>=2.31
python-dotenv>=1.0
PyJWT>=2.8

# Đọc tệp đính kèm (PDF/Word/Excel)
pypdf>=4.0
python-docx>=1.1
openpyxl>=3.1

# 🗣 Lồng tiếng + gắn phụ đề video: ffmpeg tĩnh qua pip (không cần cài đặt hệ
# thống, chạy được cả trên Render free) + giọng đọc miễn phí (Microsoft Edge TTS)
imageio-ffmpeg>=0.5
edge-tts>=6.1

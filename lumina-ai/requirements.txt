fastapi>=0.115
uvicorn[standard]>=0.30
anthropic>=0.100
httpx>=0.27
pydantic>=2.7
google-auth>=2.30
requests>=2.31
python-dotenv>=1.0
PyJWT>=2.8

# Đọc tệp đính kèm (PDF/Word/Excel) — bộ đọc NHẸ mặc định (~vài trăm KB)
pypdf>=4.0
python-docx>=1.1
openpyxl>=3.1
# Sinh FILE tổng hợp để tải về (docx dùng python-docx ở trên; pdf dùng fpdf2)
fpdf2>=2.7
# (TÙY CHỌN — bỏ comment nếu muốn đọc tệp XỊN hơn: Markdown sạch giữ bảng/tiêu đề,
#  hỗ trợ thêm .pptx/.html/.epub. Lưu ý KÉO ~35MB onnxruntime → deploy nặng hơn.
#  files.py tự dùng nếu có, không thì lùi về bộ đọc nhẹ ở trên. Tắt bằng MARKITDOWN_DISABLE=1)
#  Dùng markitdown[all] để có đủ pptx/xlsx/pdf converter.
# markitdown[all]>=0.1

# 🗣 Lồng tiếng + gắn phụ đề video: ffmpeg tĩnh qua pip (không cần cài đặt hệ
# thống, chạy được cả trên Render free) + giọng đọc miễn phí (Microsoft Edge TTS)
imageio-ffmpeg>=0.5
edge-tts>=6.1

# 🌐 Đọc nội dung trang web khi người dùng dán link — hoạt động với MỌI bộ não
beautifulsoup4>=4.12

# 🎬 Xem video qua LINK (YouTube/Vimeo/TikTok...): tải video + lấy phụ đề gốc
yt-dlp>=2024.1.0

{
  "name": "emberfall",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "EMBERFALL — a shared-world MMO where the light is a common resource.",
  "license": "MIT",
  "scripts": {
    "start": "node server/index.js",
    "test": "node --test tests/"
  }
}
